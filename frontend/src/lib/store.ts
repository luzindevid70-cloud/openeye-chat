import { create } from 'zustand'; interface Chat { id: string; title: string; updatedAt: string; } interface AppState { sidebarOpen: boolean; toggleSidebar: () => void; currentChatId: string | null; setCurrentChatId: (id: string | null) => void; darkMode: boolean; toggleDarkMode: () => void; chats: Chat[]; setChats: (chats: Chat[]) => void; }
export const useStore = create<AppState>((set) => ({ sidebarOpen: true, toggleSidebar: () => set(s => ({ sidebarOpen: !s.sidebarOpen })), currentChatId: null, setCurrentChatId: (id) => set({ currentChatId: id }), darkMode: true, toggleDarkMode: () => set(s => ({ darkMode: !s.darkMode })), chats: [], setChats: (chats) => set({ chats }) }));
